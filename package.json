{
  "name": "dali-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish",
    "clean": "pnpm -r clean",
    "format": "vp fmt",
    "lint": "vp check",
    "lint:fix": "vp check --fix",
    "prepare": "vp config",
    "crg": "test -z \"$CI\" && code-review-graph daemon add . && code-review-graph daemon start",
    "test": "vp test run",
    "test:coverage": "vp test run --coverage",
    "test:integration": "pnpm --filter @woss/dali-memory test:integration",
    "test:watch": "vp test watch",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "dotenv": "^17.4.1",
    "surrealdb": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@11.1.2"
}
