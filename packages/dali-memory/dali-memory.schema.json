{
  "title": "DaliMemoryConfig",
  "description": "Configuration schema for dali-memory plugin",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference for editor support and validation"
    },
    "storage": {
      "type": "object",
      "properties": {
        "mode": {
          "type": "string",
          "enum": [
            "embed",
            "remote"
          ],
          "description": "Storage mode: 'embed' for local embedded database, 'remote' for remote server"
        },
        "embed": {
          "type": "object",
          "properties": {
            "engine": {
              "default": "surrealkv",
              "type": "string",
              "enum": [
                "surrealkv",
                "memory"
              ],
              "description": "Embedded engine type: 'surrealkv' (persistent, default) or 'memory' (in-memory, resets on restart)"
            },
            "dataPath": {
              "type": "string",
              "description": "Path to store embedded data (required when engine is 'surrealkv'). Supports ~ for home directory. Defaults to ~/.config/dali-memory/data/"
            }
          },
          "required": [
            "engine",
            "dataPath"
          ],
          "additionalProperties": false,
          "description": "Embedded storage settings (required when mode is 'embed')"
        },
        "remote": {
          "type": "object",
          "properties": {
            "url": {
              "type": "string",
              "description": "URL of the remote memory server (WebSocket or HTTP)"
            },
            "auth": {
              "type": "object",
              "properties": {
                "username": {
                  "type": "string",
                  "description": "Username for basic authentication"
                },
                "password": {
                  "type": "string",
                  "description": "Password for basic authentication. Supports env:// and file:// prefixes for secrets"
                },
                "accessToken": {
                  "type": "string",
                  "description": "Bearer access token. Supports env:// and file:// prefixes for secrets"
                }
              },
              "additionalProperties": false,
              "description": "Authentication credentials for remote server"
            },
            "namespace": {
              "type": "string",
              "description": "SurrealDB namespace to use for memory storage"
            },
            "database": {
              "type": "string",
              "description": "SurrealDB database to use for memory storage"
            }
          },
          "required": [
            "url"
          ],
          "additionalProperties": false,
          "description": "Remote storage settings (required when mode is 'remote')"
        }
      },
      "required": [
        "mode"
      ],
      "additionalProperties": false,
      "description": "Storage backend configuration"
    },
    "embedding": {
      "type": "object",
      "properties": {
        "endpoint": {
          "type": "string",
          "description": "API endpoint for the embedding model (required for 'remote' provider). Supports OpenAI-compatible endpoints"
        },
        "model": {
          "type": "string",
          "description": "Model identifier for text embeddings. Used as model name for 'remote' provider, or HuggingFace model ID for 'local' provider (default: Xenova/bge-large-en-v1.5)"
        },
        "apiKey": {
          "type": "string",
          "description": "API key for the embedding service. Supports env:// and file:// prefixes for secrets"
        },
        "provider": {
          "type": "string",
          "enum": [
            "remote",
            "local"
          ],
          "description": "Embedding provider: 'remote' (API endpoint) or 'local' (local HuggingFace pipeline). Defaults to 'remote' for backward compatibility"
        },
        "modelCacheDir": {
          "type": "string",
          "description": "Directory to cache downloaded models (used by 'local' provider). Defaults to ~/.config/dali-memory/model_cache/"
        }
      },
      "required": [
        "endpoint",
        "model"
      ],
      "additionalProperties": false,
      "description": "Embedding model configuration"
    },
    "plugin": {
      "type": "object",
      "properties": {
        "chatMessage": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "description": "Enable capturing chat messages for memory"
            }
          },
          "required": [
            "enabled"
          ],
          "additionalProperties": false,
          "description": "Chat message memory capture settings"
        },
        "autoCapture": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "description": "Enable automatic memory capture from session activity"
            }
          },
          "required": [
            "enabled"
          ],
          "additionalProperties": false,
          "description": "Automatic memory capture settings"
        }
      },
      "additionalProperties": false,
      "description": "Plugin behavior configuration"
    }
  },
  "required": [
    "storage"
  ],
  "additionalProperties": false
}
